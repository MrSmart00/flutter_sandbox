name: app_repositories
description: "I/O package"
publish_to: 'none'
version: 0.0.1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2

dev_dependencies:
  custom_lint: ^0.7.0
  flutter_test:
    sdk: flutter

flutter:
  assets:
    - assets/
